name: Rust CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  rust-ci:
    uses: abrance/yoc/.github/workflows/rust-ci.yml@v1.0.0
    with:
      working-directory: .
      rust-toolchain: stable