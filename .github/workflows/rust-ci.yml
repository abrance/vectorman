name: Rust CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  rust-ci:
    uses: abrance/yoc/.github/workflows/rust-ci.yml@v1
    with:
      working-directory: .
      rust-toolchain: stable