[package]
name = "vectorman-metrics"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
async-trait.workspace = true
axum = "0.8"
libc = "0.2"
prometheus = "0.13"
tokio.workspace = true

[dev-dependencies]
http-body-util = "0.1"
tower = { version = "0.5", features = ["util"] }
